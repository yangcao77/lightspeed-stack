apiVersion: v1
kind: Pod
metadata:
  name: mcp-mock-server
  labels:
    app: mcp-mock-server
spec:
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: mcp-mock-server
      image: python:3.12-slim
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop: ["ALL"]
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      command: ["python", "/app/server.py", "3000"]
      ports:
        - containerPort: 3000
      volumeMounts:
        - name: server-script
          mountPath: /app/server.py
          subPath: server.py
      readinessProbe:
        httpGet:
          path: /health
          port: 3000
        initialDelaySeconds: 5
        periodSeconds: 5
      livenessProbe:
        httpGet:
          path: /health
          port: 3000
        initialDelaySeconds: 10
        periodSeconds: 10
  volumes:
    - name: server-script
      configMap:
        name: mcp-mock-server-script
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-mock-server
spec:
  selector:
    app: mcp-mock-server
  ports:
    - port: 3000
      targetPort: 3000
---
# Alias expected by MCP e2e configs and Gherkin (http://mock-mcp:3001/...).
apiVersion: v1
kind: Service
metadata:
  name: mock-mcp
spec:
  selector:
    app: mcp-mock-server
  ports:
    - port: 3001
      targetPort: 3000
